services:
  web:
    container_name: nginx
    build:
      context: ./requirements/nginx
      dockerfile: Dockerfile
    ports:
      - '443:443'
    networks:
      - inception_net

networks:
  inception_net:
    driver: bridge
